#!/bin/bash
# Official ADMIXTURE 1.3.0 (x86_64). On Apple Silicon run under Rosetta.
ROOT="$(cd "$(dirname "$0")" && pwd)"
BIN="$ROOT/admixture.x86_64"
if [[ "$(uname -m)" == "arm64" ]]; then
  exec arch -x86_64 "$BIN" "$@"
else
  exec "$BIN" "$@"
fi
