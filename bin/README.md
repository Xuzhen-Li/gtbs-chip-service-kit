# Local tools (not conda)
#
# admixture — official Alexander/Novembre ADMIXTURE 1.3.0 (macOS x86_64).
# On Apple Silicon the `admixture` wrapper runs: arch -x86_64 ./admixture.x86_64
# Source: https://dalexander.github.io/admixture/download.html
# Projection: admixture -P study.bed K   (requires study.K.P.in next to bed)
#
# Usage:
#   export PATH="/Users/lixuzhen/Desktop/script/00_array/grapeancestry_suite/bin:$PATH"
